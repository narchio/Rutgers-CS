package Chess;

/**
 * The enum Color represents the possible colors used in a chess game for pieces and squares on the board.
 * @author Nicolas Carchio
 * @author Adam Romano
 */
public enum Color {
	BLACK, WHITE;
}
